#!/usr/bin/env bash
set -euo pipefail
HOST="${HOST:-0.0.0.0}"   # << clave para Codespaces
PORT="${PORT:-8717}"
export PYTHONUNBUFFERED=1
exec python -m uvicorn app.main:app --host "$HOST" --port "$PORT"
