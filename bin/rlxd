#!/usr/bin/env bash
set -euo pipefail
HOST="${HOST:-0.0.0.0}"   # Codespaces necesita 0.0.0.0
PORT="${PORT:-8717}"
export PYTHONUNBUFFERED=1
exec python -m uvicorn app.main:app --host "$HOST" --port "$PORT"
